const currentUser = 'Kelly Brian';
const welcomeMessage = 'Welcome to Flatbook, ${Kelly Brian}!';
const excitedWelcomeMessage = welcomeMessage.toUpperCase()
currentUser.slice(0, 1);
const shortGreeting ='Welcome, ${K.slice(0, 1)}!';
